# Local development environment with Docker PostgreSQL
NODE_ENV=development
DATABASE_MODE=local

# Point Supabase client to local API server
VITE_SUPABASE_URL=http://localhost:3001
VITE_SUPABASE_ANON_KEY=local-dev-key

# Direct PostgreSQL connection (for server)
DATABASE_URL=postgresql://postgres:password@localhost:5432/commission_tracker

# pgAdmin access
PGADMIN_URL=http://localhost:8080