name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Generate DB Types and Verify
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          npx -y supabase@2.40.7 gen types typescript --project-id ${{ secrets.SUPABASE_PROJECT_ID }} > generated.types.ts 2>/dev/null
          npx prettier --write generated.types.ts
          diff -q generated.types.ts src/types/database.types.ts || {
            echo "Database types are out of sync! Run 'npm run generate:types' locally and commit the changes."
            exit 1
          }
        shell: bash

      - name: Lint
        run: npm run lint

      - name: Check for mock/test imports in production
        run: |
          if grep -R "from ['\"]@/tests" src/; then
            echo "Mock imports detected in production code"
            exit 1
          fi

      - name: Build
        run: npm run build

      - name: Test
        run: npm test -- --run
